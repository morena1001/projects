#include <stdio.h>
#include <stdlib.h>
#include <stdbool.h>
#include "morse_code_tree.h"

int main(void) {
    Tree_Init();
    Print_Tree();

    // printf("%c\r\n", root->children[1]->children[0]->children[0]->children[0]->letter);
    // printf("%d\r\n", root->is_leaf);

    // printf("%c\r\n", root->letter);

    return 0;
}
